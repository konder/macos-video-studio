# ReelForge 产品功能与场景(对焦文档)

> **本文是开发的唯一功能依据**:与用户逐节对焦确认,作为产品方向的事实来源。会话压缩后以本文为准。
> 技术契约见 native-ui.md / data-model.md / api-contract.md / agent-system.md / pipeline.md;
> 实现与本文的差异见 gap-analysis.md。
>
> 状态标记:✅ 已对齐确认 · 🟡 待确认 · ⏳ 占位/后续 · 💬 待讨论

---

## 1. 产品背景与解决的问题 ✅

ReelForge 本质是一个**视频生成的工作流软件**。把"用 AI 生成一支视频"的过程抽象成一条主线:

```
剧本  →  资产制作  →  分镜头制作  →  视频/音频剪辑合成
```

- **资产制作**:角色 / 服装 / 道具 / 场景 / 风格 等**静态资产**,各自经一条**制作流程**产出。
- **分镜头制作**:在这些资产之上,经流程**重新生成与整合**成镜头画面与视频。
- **剪辑合成**:把镜头按序拼接、配音配乐成片(⏳ 本期占位,见 §5)。

**Agent 要解决的问题**:把"从剧本拆分 → 资产/分镜设计 → 生成流程的设计与执行 → 合成"这整条链智能化。

**但 Agent 与人无特权之分**(核心原则,见 §6):同一个产品、同一套功能,既可由 Agent 完成、也可由人完成、
也可人与 Agent 交替协作——走的是**完全相同的渠道与路径**。

---

## 2. 核心抽象与工作流 ✅

| 阶段 | 产出 | 流程载体 |
|---|---|---|
| 0 剧本 | 创意/剧本 → 镜头清单 | Agent 拆分(人可改) |
| 1 资产制作 | 角色/服装/道具/场景/风格的图集 | 每个资产一条**制作流程**(§3) |
| 2 分镜头制作 | 每镜头关键帧 + 视频 take + 选片 | 在资产上**重新生成与整合**的流程(§4) |
| 3 剪辑合成 | 成片(视频+音频) | ⏳ 占位(§5) |

**"流程" = 面向技术的可视化 ComfyUI 节点图(Graph IR)** ✅。无论资产制作还是分镜生成,每个具体任务背后
都是一张节点流程图,在**技术层**(§7)可视化查看与编辑;Agent 和人改的是同一张图。

---

## 3. 资产生成(第一个核心功能)✅

### 3.1 资产抽象
- 一个资产 = **一组图片 + / 或一段文字**。
- 类型(本期固定 5 类,**先不考虑资产间关联关系**):
  **角色 / 服装 / 道具 / 场景 / 风格**。
  - (对应 data-model:Character / Wardrobe / Prop / Environment / StyleFrame)

### 3.2 资产 = 产物 + 生成流程(统一模型)✅
每个资产持有:
- **finals**:产物图集(该资产的定稿图)。
- **生成流程(Graph IR,可空)**:产出这些图的 ComfyUI 节点流程;空 = 没有可执行流程(如纯上传)。
- **元数据**:名称、文字描述/prompt、(角色)触发词、相似度阈值等。

> **统一原则**:模型上一切资产都"可挂一张生成流程图"(技术层永远能看/改),但**执行上不强求**——
> 上传图这类平凡来源,流程图可为空,不强跑 GPU。

### 3.3 模板 → 实例(每个资产一张可编辑流程)✅
- 每个**资产类型**有一条**默认模板流程**:支持"**文字**"或"**文字 + 参考图**"来生成。
- **创建一个资产时,从模板 copy 出一份实例**(= 该资产自己的 Graph IR),之后可随时增删节点。
  - 例:默认模板是"文字+参考图";创建时不给参考图 → 等价于删掉参考图节点 → 退化成纯文生图。
- 实例属于该资产,Agent 与人都在技术层改这张图(同一套 op,§6)。

### 3.4 创建方式(最小实现)✅
1. **上传图片** → finals 直接落库,流程图可空。
2. **文字生成** → 文生图流程(char_concept / Z-Image),产出 1 张图。
3. **文字 + 参考图生成** → 参考编辑流程(keyframe_edit / Qwen-Image-Edit,参考图=输入),产出 1 张图。

**角色 = 严格 3 张单人全身图(分 3 次生成)**:
- 不在一张图里塞多视角("character sheet/turnaround/multiple angles" 会塞多个人、数量不可控 → 出 4 个/很多)。
  改为 `character_view_prompts` **分 3 次各出一张单人全身图**(front/side/back),每张 `solo, single person,
  one character only, full body, standing` → 合起来**正好 3 张**。画风随 project.meta.style。
- 单视尺寸竖幅 832×1216(asset_dims),保存在 asset.width/height。
- 共享生成函数 `pipeline.generate_asset_images`(GUI 表单 / Agent / 重生成共用)。
- **Agent 的 prompt 只写身份/外观**(年龄性别/面部发型/服装/配饰),禁姿势/镜头/画风/背景/三视角
  (尤其 sitting/illustration),由系统统一加。
- 其余类型:单张文生图,原 prompt + 1024×1024。

**其它**:
- **随机 seed**:每次生成/重生成随机 seed,"重新生成"= 出新变体。
- **尺寸可改重生成**:资产详情「生成提示词」卡展示并可编辑 width×height,重生成按新尺寸(`/regenerate {prompt?,width?,height?}`,保存)。
- **图集展示**:FinalsView —— 1 张大图;多张(角色三视角)并排,scaledToFit 完整看清。

> **基模取舍(已定)**:编辑类基模必须有输入图,故"删参考图退化为文生图"在**最小实现里由
> "创建时是否给参考图"决定走哪条预设流程**(给=编辑流程,不给=文生流程);技术层手动删节点到跑不通时
> validate 会报错(用户的编辑权,符合对称原则)。

### 3.5 各类资产产出规格(按类型分别建模)✅
- **角色**:3 张单人全身图(正/侧/背),竖幅 832×1216。
- **服装**:3 视(正/背/侧)**单独服装**,ghost mannequin 无人、纯白底,896×1152。
- **道具**:3 视(正/侧/3-4)单物产品图,白底,1024²。
- **场景**:单张空镜横幅(no people),1344×768。
- **风格**:单张 moodboard,1024²。
- 统一由 `recipes.asset_view_prompts(atype,prompt,style)` + `_type_base`/`_VIEWS` 生成(关键:服装/道具不画人、场景不画人)。
- **角色 LoRA 默认不做**。

### (旧)3.5b 备注 ✅
- 一律:**按输入(文字 / 文字+参考图)产出 1 张图**。
- 角色:用文字+参考图可**一次性生成一张含正/侧/背三视角的图**(靠 prompt,不展开 LoRA)。
- 场景/服装/道具/风格:同最小设定,后续再加多视角/投影图等专门规格。
- **角色 LoRA 默认不做**(用"文字+参考图一次出三视角"替代)。

### 3.6 待确认 🟡
- 后续扩展:角色三视角的分辨率/张数、场景平面投影图种类等专门产出规格。

### 3.7 实现状态(已落地 + 5090 验证)✅
- **创建**:三种方式(上传 / 文字 / 文字+参考图)。
  - 后端 `POST /projects/{name}/assets/generate`(有参考图→keyframe_edit,无→char_concept),
    实例化的图作为 `asset.graph` 落库,产物入 `finals`;异步作业(JOBS,轮询 /jobs)。
  - 上传 `POST /projects/{name}/upload` → `create_character`/`create_asset` op(带 graph/prompt)。
- **技术层流程可查/可改**:节点画布泛化为 `GraphRef{kind,id}`(shot|asset|character 同一画布);
  `GET /projects/{name}/assets/{id}/graph` 取流程;改参/删节点经 `/projects/{name}/ops`
  (图级 op 目标对称:shot/asset/character),进统一历史、可撤销(inverse 保目标键)。
- **重新生成**:`POST /projects/{name}/assets/{id}/regenerate` 按当前(可能已编辑)流程重出图,
  更新 finals(op 入历史)。
- **客户端**:BibleStrip「新建」表单(类型+方式);资产/角色详情「生成流程(节点图)」+「重新生成」按钮。
- **Agent 也能建资产(对称)**:搭图 Agent 用 `create_asset(atype,name,prompt)` 工具,"帮我生成一个角色"→
  与人用表单同构地走 create-first 流程。
- **多轮对话**:`/chat` 带 history(近 10 轮),Agent 记得上下文;用户细化上一轮(如"我意思是办公室工服")时,
  Agent 用 `list_assets` 找到该资产 + `update_asset(id,prompt)` 改并重生成,**不新建**。
- **回复只给自然语言**:系统提示约束 Agent 不输出表格/JSON/markdown,只一两句话说清做了什么+在哪+可继续做什么。
- **react 过程可见**:前端把工具调用逐步渲染(检索配方→…→执行出图,spinner→✓)。
- **粘贴图片**:对话框粘贴图→上传→作参考附给 Agent(`/chat` image 字段)。
- **create-first 顺序(已修正)**:创建 = **先建实体(入左侧树,finals 空+已挂流程)→ 执行 → 回填预览**。
  GUI 表单立即返回 asset_id、实体立现;Agent 路径在 tool_result 时客户端实时刷新树/预览。
- **提示词保存/展示/改词重生成**:资产存用户原始 prompt;详情页「生成提示词」卡可查看/编辑,
  「重新生成」按当前(可编辑)prompt 重建流程并保存(`POST /assets/{id}/regenerate {prompt?}`,
  角色仍套三视角白底增强)。
- **图片居中放大展示**:资产/角色详情用 BigImage(scaledToFit,不裁剪,~560×320 居中),
  三视角宽图可完整查看;多图时下方小缩略条。
- 验证:文字生成 styleframe/prop → finals+graph;asset 目标 set_param(steps 8→6,inverse 正确)→流程更新。
- 提交:3e09ee4 / ac9073d / 3add626(见 git log)。

---


### 3.8 资产管理增强(2026-06)✅
- **左栏结构**:项目 = 概览 / 资产(角色·服装·道具·场景·**风格**·自定义)/ 分镜 / 成片;每个资产子组行内「＋」按该组类型新建(角色走三视角,自定义可填类型名)。风格(styleframe)作为固定组加回。
- **删除**:资产/角色详情、镜头详情均有「删除」(走 delete_asset/delete_character/delete_shot op,产物文件保留)。
- **图片放大**:资产图点击全屏查看(lightbox)。
- **组合资产**:`POST /projects/{name}/assets/compose {name,asset_ids,prompt}` —— 选 2–3 个资产(角色/服装/道具/场景)作多图参考,Qwen-edit(keyframe_compose)合成一张新资产(type=composed,落「自定义/组合」),create-first + 异步回填。
- **概览=项目**:点项目行即选中概览(项目与概览同时高亮)。

### 3.9 组合资产 + 多轮 Agent(2026-06)✅
- **透明背景**:资产产物经 rembg 后处理统一抠成**透明 PNG**(角色/服装/道具/人物卡片;场景/风格保留背景)。`pipeline.strip_bg`。
- **「人物卡片」**:原「自定义」组明确为**人物卡片**——角色+服装+道具的聚合体(=组合资产,type=composed);其「＋」直接走组合流程。
- **人物卡片(原「自定义」)**:= 角色三视 + 服装/道具细节的**拼版索引卡**(单张,`pipeline.compose_card` 用 PIL 拼图,
  **不重渲染**),供后续视频生成统一参考。`POST /assets/compose`:asset_ids[0]=角色(必选,取多视图),其余=组件(各取首图);
  type=composed。详情显示「组成(来源资产)」可点回组件。交互:角色单选必选 + 服装/道具可选。
- **场景移到分镜**:`场景(environment)` 从「资产」移到「分镜」分组下(仍是 asset type=environment,只改归属位置)。
- **Agent 多轮**:`/chat` 带近 20 轮历史;`list_assets`/`update_asset` 工具让细化走"改已有"而非新建;
  工具循环上限 30 迭代(复杂任务多次调工具);回复只给自然语言。
- **多会话**:右栏 Agent 支持多个 session 切换/新建/删除(按项目本地持久化 localStorage),首条消息作标题。

## 4. 分镜头制作 ⏳(下一节对焦)
在资产之上,经流程重新生成与整合:**场景(environment)归此** → 关键帧 → 视频 take → 选片。(详细待对焦)

## 5. 视频/音频剪辑合成 ⏳(占位)
本期占位。已有最小实现:按选定 take 顺序拼接 + 导出 1080p 片段 + FCPXML(给外部 NLE 收尾)。
完整剪辑/调色/字幕/音轨/口型范围待定。

---

## 6. Agent × 人:同一产品,对称协作 ✅
**单模型,多操作端,对称写入**(native-ui 头号原则):
- Project / Graph IR 是唯一事实来源(名词);Agent、人、未来客户端都只是"操作端"(动词入口)。
- 任何写操作都走**同一套 op**,落进同一份模型 + 同一条历史。Agent 无特权写路径。
- 三种方式同一产品内共存:全 Agent 完成 / 全人手动 / Agent 起手人微调 / 交替接管(镜头粒度持笔)。

## 6.5 客户端形态:Web 为主(2026-06 决策)✅
- 由 SwiftUI 原生客户端切换为 **Web 客户端为主**(SwiftUI 搁置,代码保留)。理由:HTML/CSS 出漂亮 UI 更快、
  节点画布(SVG)更易;且方案本就规划"多客户端接同一套 op API"——**后端 100% 复用,只换前端**。
- 技术栈:**零构建**单页 `orchestrator/app/web/index.html`(Tailwind CDN + 原生 JS,节点画布用 SVG)。
- 托管:FastAPI `app.mount("/app", StaticFiles(html=True))`,**同源免 CORS**;浏览器开 `http://<host>:8000/app/`。
- 视觉:还原 docs/mockups.md 浅色 macOS 风(浅灰底 + 白卡片 + 系统蓝强调 + 资产按类型配色)。
- 迭代:去 mockups 配色精修(零阴影/四级灰阶/SVG图标);左栏=概览(=项目行)/资产(角色·服装·道具·场景·风格·自定义)/分镜/成片,每组行内＋新增;Agent 对话单栏(去历史tab)+ react 过程逐步渲染 + 粘贴图片附参考(/chat image 字段);资产删除/图片全屏/组合资产。
- 首版覆盖:连接/项目树、资产画廊·详情(FinalsView/提示词/尺寸/重生成)·新建(上传/文字/文字+参考图)、
  分镜板、Agent 流式对话(SSE)+历史、节点画布(查看+改参 op)、选片/导出。后续迭代打磨。

## 7. 两层视图 ✅
- **导演层**:制片管理面,围绕分镜头(任务进度/预览/引用资产)。不是节点画布。
- **技术层**:全应用唯一的画布 = 某个具体任务(资产生成 / 分镜生成)的 **ComfyUI 节点流程图**;
  Agent 与人都在这里改"流程"。

## 8. 端到端场景 ⏳(待补)

## 9. MVP 范围与非目标 ⏳(随对焦累积)
- 占位:视频/音频剪辑合成(§5);资产间关联关系(本期不做)。

## 10. 与现有实现的差异 → 见 gap-analysis.md
对焦定稿后,逐条把实现纠偏到本文。
